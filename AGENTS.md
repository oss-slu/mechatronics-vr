# Repository Instructions

This repository uses the `.agents/` directory for project-specific agent guidance.

Before making changes, read the files in `.agents/` and follow those instructions as the source of truth for AI-assisted work.

- Primary instructions directory: `.agents/`
- If multiple files are present, apply all relevant guidance in that folder.
